import { defineConfig } from 'dumi';

const repo = 'Youmi.github.io';
export default defineConfig({
  base: '/Youmi.github.io',
  publicPath: '/Youmi.github.io',
  themeConfig: {
    name: 'Youmi',
  },
});
